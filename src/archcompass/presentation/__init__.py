"""Presentation adapters.

The two ways in: `cli/` for someone at a terminal, `web/` for the workspace in a browser.
Neither holds application logic — both open a `Runtime` and call services on it.
"""
