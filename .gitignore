.venv/
.env
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
# The frontend build output. Not committed: the build backend packs this directory from
# disk rather than from git, and every target that serves or ships it (`web`, `build`,
# `test-browser`) builds it first, so the wheel carries a fresh bundle either way. When it
# is absent the server answers `frontend_not_built` instead of failing to start.
src/archcompass/presentation/web/static/
.archcompass/
# This repository is also a workspace, so running `archcompass web` here writes the
# workspace's own configuration from the packaged template. The configurations that ship
# are named for the provider they point at (`models.ollama.yaml`, `models.google.yaml`);
# this one is local state.
config/models.yaml
reports/
frontend/node_modules/
frontend/coverage/
frontend/*.tsbuildinfo
# Local agent orientation, kept out of the repository.
.agents/
.claude/

*.log
*.impeccable/*